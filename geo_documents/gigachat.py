from __future__ import annotations

import json
import urllib.error
import urllib.request
from dataclasses import dataclass

DEFAULT_GIGACHAT_URL = "http://91.146.28.240:8041/api/gigachat/simple"
DEFAULT_MAX_TOKENS = 100_000
MAX_REPORT_TEXT_CHARS = 150_000

EXPLANATORY_NOTE_TITLE = (
    "2. Пояснительная записка по инженерно-геологическим изысканиям"
)

# Внутренний чек-лист для модели (не выводить в текст записки дословно).
_EXPLANATORY_NOTE_CHECKLIST = """
1.1 Введение: наименование объекта; исполнитель; цели вида работ; цели и задачи
1.2 Изученность: материалы ранее выполненных изысканий
1.3 Физико-географические и техногенные условия: административное и геоморфологическое
  положение; климат; снеговой район по СП 20; нормативная глубина промерзания; техногенные условия
1.4 Методика: категория сложности; число и номера скважин; вид привязки; способ и установка бурения;
  отбор проб (объём, глубины, маркировка); какие ГОСТ/СП применялись; какие испытания, где и на каких приборах;
  объёмы работ (п.м., шт., лаб. определений)
1.5 Геологическое строение: глубина бурения; состав разреза; основание выделения ИГЭ; изменение мощностей ИГЭ
1.6 Специфические грунты: виды; скважины вскрытия; мощности
1.7 Гидрогеологические условия: горизонты; тип и породы; химсостав вод; подтопляемость по СП 11; прогноз УГВ
1.8 Свойства грунтов: перечень ИГЭ; границы; физико-механические свойства; пучинистость; коррозионная агрессивность
1.9 Процессы: опасные процессы; сейсмичность; суффозия
1.10 Контроль и приёмка: внутренний и наружный контроль
1.11 Заключение: краткие выводы по разделам 1.1–1.10; рекомендации только по строительству и проектированию
""".strip()

OUTPUT_FORMAT_EXAMPLE = """
2. Пояснительная записка по инженерно-геологическим изысканиям

@Р@ 1.1 Введение

@П@ Наименование объекта
Текст абзаца с конкретными данными.

@П@ Исполнитель
ООО «…», договор № … от …, основание работ — …

@Р@ 1.4 Методика и технология выполнения работ

@П@ Отбор проб грунта
Отбор монолитов и образцов нарушенной структуры выполнен из скважин Скв.1–Скв.5
с интервалов 2,0–8,0 м, 8,0–15,0 м. Применены ГОСТ 12071-2014, ГОСТ 5180-2015.
Образцы доставлены в лабораторию ООО «Геотест», физико-механические испытания — по ГОСТ 12248.6-2020
на прессе ПГ-100, срез — на приборе сдвига … .

@Р@ 1.11 Заключение

@П@ Краткие выводы
Текст по разделам 1.1–1.10.

@П@ Рекомендации
1) Применить свайный фундамент с учётом пучинистых грунтов.
2) Устроить вертикальную планировку с отметкой реза.
""".strip()


@dataclass
class GigaChatResponse:
    content: str
    used_tokens: int


def call_simple(
    text: str,
    *,
    url: str = DEFAULT_GIGACHAT_URL,
    max_tokens: int = DEFAULT_MAX_TOKENS,
    timeout_sec: int = 300,
) -> GigaChatResponse:
    """Отправляет текст на /api/gigachat/simple и возвращает ответ модели."""
    payload = {"text": text, "maxTokens": max_tokens}
    data = json.dumps(payload, ensure_ascii=False).encode("utf-8")
    request = urllib.request.Request(
        url,
        data=data,
        headers={"Content-Type": "application/json; charset=utf-8"},
        method="POST",
    )

    try:
        with urllib.request.urlopen(request, timeout=timeout_sec) as response:
            raw = response.read().decode("utf-8")
    except urllib.error.HTTPError as exc:
        body = exc.read().decode("utf-8", errors="replace")
        raise RuntimeError(f"GigaChat HTTP {exc.code}: {body}") from exc
    except urllib.error.URLError as exc:
        raise RuntimeError(f"GigaChat недоступен: {exc.reason}") from exc

    try:
        parsed = json.loads(raw)
    except json.JSONDecodeError as exc:
        raise RuntimeError(f"Некорректный JSON от GigaChat: {raw[:500]}") from exc

    content = str(parsed.get("content", "")).strip()
    used_tokens = int(parsed.get("usedTokens") or 0)

    if not content:
        raise RuntimeError("GigaChat вернул пустой ответ.")
    if content.startswith("Ошибка при обращении"):
        raise RuntimeError(content)

    return GigaChatResponse(content=content, used_tokens=used_tokens)


def build_explanatory_note_prompt(report_text: str) -> str:
    """Формирует промпт для пояснительной записки в формате типового ИГИ."""
    trimmed = report_text.strip()
    if len(trimmed) > MAX_REPORT_TEXT_CHARS:
        trimmed = trimmed[:MAX_REPORT_TEXT_CHARS] + "\n\n[... текст сокращён ...]"

    return (
        "Составь пояснительную записку по инженерно-геологическим изысканиям.\n"
        "Записка вставляется в отчёт сразу после «Содержания» как раздел 2.\n\n"
        "ПРАВИЛА:\n"
        "1. Используй ТОЛЬКО сведения из текста материалов отчёта ниже. "
        "Не придумывай данные и не опирайся на общие знания.\n"
        "2. Пиши МАКСИМАЛЬНО ПОДРОБНО и КОНКРЕТНО — как в официальной пояснительной записке ИГИ:\n"
        "   • каждый подпункт — 2–5 абзацев с фактами из материалов;\n"
        "   • указывай номера скважин, глубины, мощности, отметки, координаты, даты, "
        "наименования организаций, марки установок, виды работ в объёмах (п.м., шт.);\n"
        "   • для нормативов — ПОЛНОЕ обозначение: «ГОСТ 12071-2014», «СП 47.13330.2016», "
        "«СП 11-105-97 ч. II» и т.д., как в тексте отчёта;\n"
        "   • для испытаний — где проводились, какие определения, на каких приборах.\n"
        "3. ЗАПРЕЩЕНЫ общие фразы без конкретики. Не пиши:\n"
        "   • «в соответствии с требованиями ГОСТ» — укажи КАКИЕ ГОСТ/СП;\n"
        "   • «в соответствии с нормативными документами», «согласно действующим нормам»;\n"
        "   • «лабораторные испытания проводились» — укажи ГДЕ, ЧТО, ПО КАКИМ ГОСТ;\n"
        "   • «бурение выполнено установкой» — укажи тип установки, диаметр, глубины;\n"
        "   • «отбор проб произведён» — укажи из каких скважин, с каких отметок, сколько образцов.\n"
        "   Если в материалах нет конкретного ГОСТ/цифры — не выдумывай; "
        "пиши: «Сведения в представленных материалах не приведены.»\n"
        "4. Если данных по подпункту нет — одна строка: "
        "«Сведения в представленных материалах не приведены.»\n"
        "5. Не используй «вероятно», «предположительно», «обычно», «как правило».\n"
        "6. Не добавляй ссылки на источники, номера источников и списки источников.\n\n"
        "РАЗДЕЛ 1.11 — ОСОБЫЕ ПРАВИЛА:\n"
        "• «Краткие выводы» — по разделам 1.1–1.10 на основе материалов отчёта.\n"
        "• «Рекомендации» — СФОРМУЛИРУЙ САМ: 3–7 пунктов «1) … 2) …» "
        "ТОЛЬКО по строительству и проектированию.\n"
        "• ЗАПРЕЩЕНО в рекомендациях: повторные изыскания, дополнительное бурение, "
        "«провести дополнительные исследования».\n\n"
        "ФОРМАТ ВЫВОДА:\n"
        f"- Первая строка: «{EXPLANATORY_NOTE_TITLE}»\n"
        "- Раздел: «@Р@ 1.X Краткое название»\n"
        "- Подпункт: «@П@ Краткий подзаголовок» (2–5 слов, без «?»)\n"
        "- После подзаголовка — развёрнутый текст: 2–5 абзацев с цифрами и обозначениями\n"
        "- Между разделами — пустая строка\n"
        "- Без Markdown (#, **)\n"
        "- Не включай чек-лист и этот промпт в ответ\n\n"
        "Пример оформления:\n"
        f"{OUTPUT_FORMAT_EXAMPLE}\n\n"
        "Содержание разделов (раскрой все темы максимально подробно):\n"
        f"{_EXPLANATORY_NOTE_CHECKLIST}\n\n"
        "Текст исходных материалов отчёта:\n"
        f"{trimmed}"
    )
